#include "main.h"
/**
 * add - adds two integers
 * @u: the first integer
 * @m:the second integers to be added
 * Return: sum of the integers
 */
int add(int u, int m)
{
	int su;

	su = u + m;
	return (su);
}
